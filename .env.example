NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_DEFAULT_LANG=fa
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change_me_to_a_long_random_value_at_least_32_chars

# Google OAuth (only external dependency)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Allowed Gmail addresses for admin panel access
ADMIN_EMAILS=admin.one@gmail.com,admin.two@gmail.com,admin.three@gmail.com
EDITOR_EMAILS=
CONTRIBUTOR_EMAILS=

# Database / queue
DATABASE_URL=postgresql://vista:change_this_strong_pass@postgres:5432/vista_ai_news
QUEUE_DATABASE_URL=
JOB_QUEUE_SCHEMA=pgboss

# Internal API access (worker <-> app)
INTERNAL_API_URL=http://web:3000
INTERNAL_API_TOKEN=change_this_internal_token

# Advanced classification (local ONNX model path)
TREND_MODEL_PATH=./models/topic-classifier.onnx

# Media storage (local filesystem path inside container)
MEDIA_UPLOAD_DIR=public/media

# Translation providers (LibreTranslate runs locally via docker-compose)
LT_URL=http://libretranslate:5000/translate
OPENAI_API_KEY=
GOOGLE_PROJECT_ID=
GOOGLE_APPLICATION_CREDENTIALS=

# Alerts
ALERT_EMAIL=devcodebase.dec@gmail.com

# SMTP configuration for newsletter/error alerts (local Mailhog or external SMTP)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_SECURE=false
SMTP_FROM="Vista AI News <news@vista-ai.local>"

# Optional default recipients for the newsletter API (comma separated)
NEWSLETTER_RECIPIENTS=

# Web push notifications
NEXT_PUBLIC_WEB_PUSH_VAPID_PUBLIC_KEY=
WEB_PUSH_VAPID_PUBLIC_KEY=
WEB_PUSH_VAPID_PRIVATE_KEY=
WEB_PUSH_CONTACT_EMAIL=mailto:devcodebase.dec@gmail.com
